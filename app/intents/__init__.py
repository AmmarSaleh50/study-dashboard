"""Intents package — user-scoped entry points for REST and MCP callers.

Phase 0: thin pass-throughs to app.services.*. The user_id parameter is
accepted but ignored until Phase 2 wires up per-user filtering.
"""
